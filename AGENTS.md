# Biped Control Lab 執筆・編集規約

このファイルは、このリポジトリを編集する人間およびAIエージェント向けの**正本となる執筆規約**です。
理論ノート、実験、図、ルーティングを変更する前に必ず読んでください。

## 1. 教材の目的

この教材は、二足歩行制御を「数式だけ」「デモだけ」に分離せず、

1. 物理モデルを置く
2. 式を導く
3. 制御則を作る
4. ブラウザ実験で壊す
5. 観察結果を式へ戻して説明する

という往復で理解するための教材です。

第1版の中心は 2D の簡略モデルです。実機制御の完全な設計書ではありません。

## 2. 想定読者

本文は、次を履修済みの大学1年程度の読者を基準にします。

- 1変数の微分・積分
- 初歩的な多変数微分
- Taylor 展開の考え方
- ベクトルと行列
- 2次正方行列の固有値・固有ベクトル
- Newton の運動方程式
- 力、トルク、仕事、エネルギー

一方、次の知識は**既知として扱ってはいけません**。

- 制御工学
- 状態空間表現
- 可制御性・可観測性
- Lyapunov 安定性
- Riccati 方程式
- 最適制御・動的計画法
- ZMP / CoP / LIPM / Capture Point
- ロボットの接触力学

これらを使う場合は本文で導入するか、「補講」として前提から説明します。

## 3. 理論ノートの必須構成

`docs/theory/*.md` を新規作成・大幅改稿するときは、原則として次の順序を守ります。

1. **前提** — この章で使う既習事項
2. **この章のゴール** — 読了後に説明・計算できること
3. **直感** — 何が問題で、なぜそのモデルを考えるのか
4. **モデル化の仮定** — 何を残し、何を捨てたか
5. **導出** — 途中式を飛ばさず、力学・数学から式を作る
6. **数式の意味** — 各項が物理的に何を表すか
7. **安定性または制御上の意味** — 「式が出た」で終わらせない
8. **実験との対応** — Lab で何を変え、何を観測するか
9. **補講** — 本筋を越える発展事項
10. **次章への接続** — 何が未解決で、次章が何を解くか

章によって不要な節は統合して構いませんが、**仮定・導出・意味・実験の対応**は省略しないでください。

## 4. 数式の書き方

### 4.1 記号は初出で必ず定義する

悪い例:

> `p = x - h/g * xddot` となる。

良い例:

> 水平重心位置を $x$、重心高さを $h$、床上の ZMP 位置を $p$ とする。重心高さ一定の LIPM では ...

### 4.2 等号と近似を区別する

- 厳密な関係は $=$
- 線形化や近似は $\approx$
- 定義は「〜と定義する」と文章でも明示する

特に `sin(theta) ≈ theta` のような線形化を、途中から厳密な等式のように扱わないでください。

### 4.3 符号規約を固定する

角度、座標、トルク、床反力の正方向は図または文章で先に定義します。
符号が変わると安定・不安定の結論が逆転するため、「慣例だから」で済ませないでください。

### 4.4 単位・次元を意識する

ゲインや評価関数を説明するときは、必要に応じて単位を確認します。
異なる単位を持つ状態量に同じ重みを付ける場合は、正規化の必要性にも触れてください。

### 4.5 安定という語を雑に使わない

可能なら次を区別します。

- 安定 / 不安定
- 漸近安定
- 線形化モデル上で局所的に安定
- 入力飽和や接触制約まで含めた実装上の安定

たとえば P 制御だけの理想倒立振子は、条件によって振動系にはできますが、減衰がなければ原点へ漸近収束しません。

## 5. 発展事項は「補講」に分離する

本文の流れを壊す内容は、`## 補講` または `### 補講: ...` に分離します。

補講の例:

- Taylor 展開から見た線形化
- 2次系の固有値・減衰比
- 可制御性・可観測性
- Lyapunov 関数
- Bellman 原理と Riccati 方程式
- 接触レンチ・摩擦円錐
- DCM / Capture Point
- Preview control / MPC

補講では、

1. なぜ本編では不要なのか
2. 何が分かるようになるのか
3. どの前提知識を追加で使うのか

を最初に短く説明してください。

## 6. 現在の記号規約

章をまたいで、できるだけ次を維持します。

- $g$: 重力加速度
- $m$: 質量
- $l$: 倒立振子の支点から質点までの長さ
- $h$: LIPM の一定重心高さ
- $\theta$: 倒立振子の直立からの傾き
- $u$: 制御トルクまたは制御入力
- $x$: 状態ベクトル。LIPM 章では水平重心位置にも使うので、初出でスカラーであることを明示する
- $A, B$: 状態方程式のシステム行列・入力行列
- $K$: 状態フィードバックゲイン
- $Q, R$: LQR の状態・入力重み
- $p$: ZMP / 簡略モデル上の床反力作用位置
- $\omega=\sqrt{g/h}$: LIPM の固有角速度
- $\xi$: Capture Point / divergent component の位置

記号を変える場合は、その章だけの都合で変更せず、関連章と Lab の実装も確認してください。

## 7. 図の規約

- 図は原則として `docs/assets/` 以下の**リポジトリ内ファイル**を参照する
- 外部画像 URL に依存しない
- Markdown の相対リンク切れを作らない
- SVG を変更した場合は、コードだけ見て終わらず**実際の描画結果を確認する**
- 小さい画面でも文字が図枠からはみ出さないか確認する
- 数式記号と図中の記号を一致させる
- 教材図は再現性を優先し、AI画像生成による説明図へ置き換えない

図は「飾り」ではなく、符号、座標、力、状態遷移を固定するために使います。

## 8. Web / SPA リンクの規約

理論ページは `docs/js/routes.js` のルートから SPA として表示されます。

- 理論 Markdown を追加したら `routes.js` へ登録する
- 章間リンクを、生の `.md` がブラウザに表示される状態にしない
- Lab へのリンクは公開サイト上で解決できる相対 URL にする
- 画像リンクも `docs/` を公開ルートとして解決できることを確認する

ルーティング規約を変えた場合は `scripts/check-routing.mjs` も同時に更新してください。

## 9. Lab と理論の整合性

理論式と JavaScript 実装のモデルが異なる場合は、どちらかを黙って合わせるのではなく差を明記します。

確認事項:

- 状態変数の定義
- 正方向
- パラメータ $m,l,h,g$
- 積分刻み
- 入力飽和
- ZMP 飽和
- ステップ判定
- 表示している値の単位

理論上可能でも Lab で不可能な操作がある場合は、UI 制約かモデル制約かを説明してください。

## 10. 文章上の禁止事項

次の書き方を避けます。

- 新しい専門語を定義せず突然使う
- 「明らか」「自明」「簡単に分かる」で途中を飛ばす
- 導出なしに公式だけ置く
- 厳密解と近似解を混同する
- 「LQR は最適だから最強」のような無条件の一般化
- ZMP が支持多角形内なら常に転倒しない、といった前提抜きの断言
- 実機ヒューマノイドへ、そのまま適用できるように読める表現

## 11. 変更前後の確認手順

理論・図・Lab を変更したら、最低限次を行います。

```bash
npm install
npm test
python -m http.server 8000 -d docs
```

そのうえでブラウザから関連章と Lab を実際に開きます。

特に確認するもの:

- KaTeX の数式崩れ
- Markdown の表・リスト崩れ
- 図の文字切れ・はみ出し
- 次章リンクが SPA 内で遷移するか
- Lab の説明と表示値が一致するか

CI が通ることは必要条件ですが、**見た目の確認を代替しません**。

## 12. AI エージェントへの追加指示

AI がこのリポジトリを編集するときは、変更対象ファイルだけを読んで即編集してはいけません。

最低限、次を読むこと:

1. この `AGENTS.md`
2. `README.md`
3. 変更対象の理論章
4. 前後1章
5. 関連する Lab の HTML / JavaScript
6. 関連図がある場合はその SVG

大きな改稿では、まず「読者がどこで新概念に出会うか」を追跡し、未定義語・未導出式・前提の飛躍を洗い出してから編集してください。

**短くすることより、読者が自力で式を再構成できることを優先します。**

## 13. 独立補講の規約

本編の1節では収まりにくい発展事項は、`docs/theory/SNN_topic.md` の形式で独立補講にします。SPA上のIDは `SNN` とします。

独立補講を追加するときは必ず次を行います。

1. 冒頭に**対象・前提・ゴール**を書く
2. 本編のどの疑問を解消する補講かを明示する
3. 大学院レベルの一般論を網羅するより、二足歩行制御への接続を優先する
4. 最後に**本編との対応**を置き、関連章へ戻れるようにする
5. `docs/js/routes.js` に `SNN` ルートを登録する
6. 本編の必須学習順序に補講を混ぜない
7. 補講同士の依存がある場合は、先に読む補講を明記する

補講は難しい内容を捨てる場所ではありません。**本編の読者レベルを守りながら、理論的な深さを失わないための第2レイヤー**として使います。
